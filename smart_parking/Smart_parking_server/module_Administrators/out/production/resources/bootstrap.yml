#  bootstrap为系统级别的配置 application为用户级配置
spring:
  cloud:
    config:
      uri: http://localhost:9999
      name:  SmartParkingServer  #需要从gitee上读取的资源名称，不需要后缀
      profile: data    #生产项
      label: master   # 分支

